import { LeagueType } from '@/types'
import React from 'react'

interface PropsType {
  league: LeagueType
}
const LeagueFixtures = ({ league }: PropsType) => {
  return <React.Fragment></React.Fragment>
}

export default LeagueFixtures
